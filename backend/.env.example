# MongoDB Connection URI
# Replace with your MongoDB Atlas URI or local MongoDB instance
# 
# Local MongoDB format:
# MONGODB_URI=mongodb://localhost:27017/hrms_lite
#
# MongoDB Atlas format:
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/hrms_lite?retryWrites=true&w=majority
#
# To get your MongoDB Atlas URI:
# 1. Go to MongoDB Atlas dashboard
# 2. Click "Connect" on your cluster
# 3. Choose "Connect your application"
# 4. Copy the connection string
# 5. Replace <password> with your database user password

MONGODB_URI=mongodb://localhost:27017/hrms_lite
